import 'package:flutter/material.dart';

class ResetPassword extends StatelessWidget {
  ///final id
  static final id = '/ResetPassword';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('data'),
      ),
    );
  }
}
